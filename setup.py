from setuptools import setup, find_packages

setup(
    name="ml-utils",
    version="0.1",
    packages=find_packages(),
    description="",
    author="Jason Li",
    install_requires=["matplotlib", "numpy"],
)
